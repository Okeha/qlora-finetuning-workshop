model_name: Qwen/Qwen3.5-0.8B

lora_hyperparameters: 
  rank: x
  alpha: x
  dropout: x
  target_modules: x

training:
  batch_size: x
  num_epochs: x
  learning_rate: x
  weight_decay: x
  warmup_steps: x
  logging_steps: x
  save_steps: x
  output_dir: x 